#include "main.h"

/**
 * _isupper - write a function that checks for uppercase character
 * @c: is a character
 * Return: 1 if its and uppercase
*/
int _isupper(int c);
{
	if (c >= A && c <= Z)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

